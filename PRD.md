# PRD — AI 감성 분석 웹 서비스

## 1. 제품 한 줄 설명
사용자가 입력한 문장을 OpenAI API로 분석하여 **긍정 / 부정 / 중립** 중 하나를 보여주고, **모델 신뢰도(백분율)**와 **분석 이유**를 함께 제공하는 간단한 웹 서비스.

---

## 2. 문제 정의
사용자는 짧은 문장을 보고 그 문장의 전반적인 감성이 긍정인지 부정인지 중립인지 빠르게 확인하고 싶다.

MVP의 목표는 복잡한 분석 도구가 아니라 다음 질문에 답하는 것이다.

> "이 문장은 어떤 감성으로 분류되었고, 왜 그렇게 판단했으며, 결과를 얼마나 확신하는가?"

---

## 3. 목표
### Must
1. 사용자가 텍스트를 입력할 수 있다.
2. 사용자가 분석 버튼을 누르면 감성 분석이 실행된다.
3. 결과는 긍정/부정/중립 중 하나로 표시된다.
4. 0~100% 형태의 신뢰도를 표시한다.
5. 1~3개의 분석 이유를 표시한다.
6. 오류가 발생하면 사용자에게 이해 가능한 오류 메시지를 표시한다.
7. 분석 결과를 Supabase에 저장한다.
8. 데스크톱과 모바일에서 사용할 수 있다.

### Should
- 분석 중 로딩 상태 표시
- 분석 결과가 화면의 주요 시각적 초점이 되도록 구성
- 최근 분석 결과가 아니라 현재 요청의 결과만 우선 표시

### Won't
- 회원가입/로그인
- 개인별 분석 이력
- 감정 종류를 3개 이상으로 세분화
- 통계 대시보드
- 실시간 스트리밍
- 파일 업로드
- 이미지/음성 분석
- 자동 번역
- 상담/진단/의료적 해석

---

## 4. 대상 사용자
- 비전공자
- AI를 직접 다루기 어려운 일반 사용자
- 문구/리뷰/피드백의 전반적인 감성을 빠르게 확인하려는 사용자

---

## 5. 사용자 흐름
1. 페이지 접속
2. 큰 입력창에 문장 입력
3. `감성 분석하기` 클릭
4. 로딩 표시
5. 결과 카드 표시
   - 감성
   - 신뢰도
   - 분석 이유
6. 분석 결과 저장
7. 실패하면 오류 메시지 표시

---

## 6. 기능 요구사항

### FR-01 텍스트 입력
- 여러 줄 입력 가능
- 최대 2,000자
- 앞뒤 공백 제거
- 빈 입력은 제출하지 않음
- 입력 길이 표시를 선택적으로 제공할 수 있으나 필수는 아님

**완료 조건**
- 빈 입력이 서버로 전달되지 않는다.
- 2,001자 입력은 OpenAI 호출 없이 차단된다.

### FR-02 감성 분석
서버는 OpenAI API에 텍스트를 전달하고 다음 결과를 받는다.
- `positive`
- `negative`
- `neutral`

분석 기준은 "문장 전체의 전반적인 정서적 방향"으로 정의한다.

**완료 조건**
- 세 감성 중 정확히 하나가 반환된다.
- 스키마 검증에 실패하면 정상 결과로 처리하지 않는다.

### FR-03 신뢰도
- 0~100 정수로 표시
- UI에는 `85%`처럼 표시
- 명칭은 `모델 신뢰도`로 한다.
- 제품 설명에서 "정확도 85%"라고 표현하지 않는다.

**중요 제약**
이 값은 모델이 해당 분류에 대해 제시한 신뢰 수준이며, 별도의 검증 데이터로 산출한 통계적 정확도/확률이 아니다.

### FR-04 분석 이유
- 1~3개
- 짧고 구체적으로
- 입력 문장에 실제로 나타난 단어/표현/맥락을 근거로 한다.
- 입력에 없는 사실을 추론하지 않는다.

예:
입력: `배송이 빨라서 정말 만족했습니다.`
결과:
- 긍정
- 92%
- `빠른 배송을 긍정적으로 평가하는 표현("정말 만족")이 포함되어 있습니다.`

### FR-05 오류 메시지
최소 다음 상태를 지원한다.
- 빈 입력
- 길이 초과
- OpenAI API 오류
- OpenAI 응답 구조 오류
- Supabase 저장 오류
- 네트워크 오류

### FR-06 저장
분석 성공 후 Supabase에 저장한다.

저장 최소 데이터:
- id
- input_text
- sentiment
- confidence
- reasons
- created_at

저장 실패 시 분석 결과 자체는 화면에 표시할 수 있다. 단, 사용자에게 저장 실패 사실을 별도로 알린다.

---

## 7. UI 요구사항

### 참고 이미지 기반 방향
첨부된 UI는 Dribbble 스타일의 디자인 탐색 서비스처럼 다음 시각적 특징을 가진다.
- 매우 넓은 흰 배경
- 큰 헤드라인
- 짙은 네이비/블랙 텍스트
- 핑크 포인트 컬러
- 둥근 pill 형태
- 넓은 검색 입력
- 카드형 콘텐츠
- 정돈된 grid
- 큰 여백과 낮은 시각적 복잡도

이 제품에서는 해당 패턴을 **감성 분석 서비스에 맞게 재해석**한다.

### 화면 구성
1. Header
2. Hero
3. 분석 입력 카드
4. 결과 카드
5. 사용 방법/제약 안내
6. Footer

### 결과 카드
- 감성 badge
- 큰 퍼센트 숫자
- 신뢰도 progress bar
- 이유 목록
- 분석 완료 시각(선택적)

---

## 8. 비기능 요구사항
### 성능
- 일반적인 단일 텍스트 분석의 서버 응답을 기다리는 동안 로딩 상태가 명확해야 한다.
- 프론트에서 OpenAI를 직접 호출하지 않는다.

### 접근성
- 입력창에 label 제공
- 버튼에 명확한 텍스트 제공
- 색상만으로 감성을 구분하지 않는다.
- 키보드만으로 입력/제출 가능
- 오류 메시지는 시각적으로뿐 아니라 접근성 트리에서도 전달

### 보안
- OpenAI API 키는 서버에서만 사용
- 민감한 키를 클라이언트에 전달하지 않음
- Supabase RLS 적용
- 원문을 로그에 무조건 남기지 않음

---

## 9. 아키텍처
```text
Browser
  │
  │ POST /api/analyze
  ▼
Vercel Node.js Function
  ├─ 입력 검증
  ├─ OpenAI API 호출
  ├─ 구조화 결과 검증
  └─ Supabase 저장
  │
  ▼
Browser
  └─ 결과 렌더링
```

---

## 10. 성공 기준
MVP는 다음 조건을 모두 만족해야 한다.

- 정상적인 긍정 문장 분석 가능
- 정상적인 부정 문장 분석 가능
- 정상적인 중립 문장 분석 가능
- 결과에 감성/신뢰도/이유가 모두 표시됨
- 오류 상황에서 오류 메시지가 표시됨
- Supabase에 성공 결과가 저장됨
- API 키가 브라우저에 노출되지 않음
- 첨부 이미지의 디자인 방향을 반영한 반응형 UI
- 테스트 케이스를 통과함

---

## 11. 검증용 대표 입력
### Positive
`이 서비스는 사용하기 쉽고 결과도 정말 만족스럽습니다.`

기대: `positive`

### Negative
`배송이 너무 늦었고 고객센터 응답도 불친절했습니다.`

기대: `negative`

### Neutral
`회의는 오후 3시에 시작하고 장소는 2층 회의실입니다.`

기대: `neutral`

### 경계 사례
`좋긴 한데 가격이 너무 비싸요.`

기대: 모델 결과를 그대로 검증하되, 요구사항은 특정 라벨을 강제하지 않는다. 복합 감정은 오판 가능성을 인정한다.

---

## 12. 완료 정의(Definition of Done)
다음 모두 충족 시 MVP 완료로 본다.

- [ ] `AGENTS.md` 규칙 준수
- [ ] 모든 Must 요구사항 구현
- [ ] 프론트/서버/DB 연동 완료
- [ ] OpenAI 구조화 출력 + 서버 스키마 검증 완료
- [ ] Supabase 저장 완료
- [ ] 오류 상태 구현
- [ ] 반응형 UI 구현
- [ ] 테스트 체크리스트 통과
- [ ] Vercel 환경변수 설정 문서화
- [ ] 실제 API 키가 저장소에 없음
- [ ] 브라우저에서 OpenAI API 키가 노출되지 않음

---

## 13. 범위 변경 규칙
다음 기능을 추가하려면 별도 요구사항으로 승인한다.
- 로그인
- 이력 조회
- 검색/필터
- 사용자별 데이터
- 다국어
- 감정 세분화
- 분석 결과 export
- 관리자 기능
- 결제

AI 에이전트는 위 기능을 "있으면 좋을 것 같다"는 이유로 구현하지 않는다.
