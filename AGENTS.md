# AGENTS.md — 감성 분석 서비스 작업 규칙

## 1. 문서의 목적
이 저장소는 사용자가 입력한 텍스트를 OpenAI API로 감성 분석하고, 결과를 화면에 표시하며 분석 기록을 Supabase에 저장하는 웹 서비스의 명세/구현 기준을 담는다.

**우선순위**
1. `AGENTS.md`: 작업 규칙과 금지사항
2. `PRD.md`: 제품 요구사항과 완료기준
3. `docs/*.md`: 세부 구현 기준
4. 실제 코드: 위 문서를 충족해야 한다.

문서에 없는 기능을 AI 에이전트가 임의로 추가하지 않는다.

---

## 2. 기술 스택 고정
- Front-End: HTML, CSS, Vanilla JavaScript
- Back-End: Node.js
- AI: OpenAI API
- DB: Supabase PostgreSQL
- Deployment: Vercel
- API 키는 브라우저에 노출하지 않는다. Node.js 서버 측에서만 사용한다.
- UI 프레임워크/React/Vue/Next.js는 별도 승인 없이는 추가하지 않는다.
- 인증, 회원가입, 관리자 페이지, 결제, 다국어, 파일 업로드는 MVP 범위에 포함하지 않는다.

OpenAI API 키는 서버 환경변수로만 관리한다. 브라우저에서 직접 OpenAI API를 호출하는 구현은 금지한다.

---

## 3. 구현 범위
### 반드시 구현
- 텍스트 입력
- 감성 분석 실행
- 긍정 / 부정 / 중립 중 하나의 결과 표시
- 신뢰도 백분율 표시
- 분석 이유 표시
- 입력 오류/서버 오류/AI 오류 표시
- 분석 결과를 Supabase에 저장
- 모바일/데스크톱 반응형 UI
- 첨부된 참고 이미지의 시각적 방향을 반영한 UI

### 구현하지 않음
- 문장의 감정 강도 점수
- 감성별 확률 분포
- 사용자별 분석 이력 화면
- 로그인/회원가입
- 관리자 대시보드
- 분석 결과 공유 링크
- PDF/CSV 다운로드
- 이미지/음성 감성 분석
- 유해 콘텐츠 판단 기능
- 심리 상태/정신건강 진단
- 모델의 결과를 사람이 검증한 것처럼 표시하는 기능

---

## 4. AI 에이전트 행동 규칙
AI 에이전트는 다음을 추측하거나 임의 결정하지 않는다.

- 존재하지 않는 API endpoint를 만들지 않는다.
- 현재 사용 중인 OpenAI SDK/API 형식을 문서 확인 없이 단정하지 않는다.
- 모델명을 임의로 고정하지 않는다. 환경변수 `OPENAI_MODEL`로 주입한다.
- Supabase 테이블/컬럼을 문서와 다르게 추가하지 않는다.
- UI의 새로운 메뉴/기능을 임의로 추가하지 않는다.
- `confidence`를 객관적인 통계 확률이라고 표현하지 않는다.
- API 실패를 정상적인 `neutral` 결과로 바꾸지 않는다.
- AI가 JSON을 잘못 반환했을 때 임의 값으로 보정하지 않는다. 검증 실패로 처리한다.
- 요구사항이 모호하면 구현 전에 문서에 명시된 기본값을 사용하고, 기본값이 없으면 작업을 중단하고 질문한다.

---

## 5. AI 응답 계약
AI 분석 결과는 다음 논리적 구조만 허용한다.

```json
{
  "sentiment": "positive | negative | neutral",
  "confidence": 0,
  "reasons": [
    "짧고 관찰 가능한 문장 근거"
  ]
}
```

제약:
- `sentiment`: 세 값 중 정확히 하나
- `confidence`: 정수 0~100
- `reasons`: 1~3개
- 각 reason은 입력 텍스트에 근거한 짧은 설명
- 이유에 입력에 없는 사실을 추가하지 않는다.
- JSON Schema/구조화 출력 기능을 사용하여 서버에서 스키마를 검증한다.
- 스키마 검증 실패 시 결과를 사용자에게 표시하지 않고 오류 상태로 처리한다.

---

## 6. 입력 검증
- 빈 문자열: 거부
- 앞뒤 공백: 제거 후 검사
- 기본 최대 길이: 2,000자
- 길이 제한을 초과하면 OpenAI 호출 전에 오류 표시
- HTML을 입력값으로 해석하지 않는다.
- 결과의 reason은 `textContent`로 렌더링하고 HTML로 삽입하지 않는다.
- 브라우저에서의 검증과 서버에서의 검증을 모두 수행한다. 서버 검증을 최종 기준으로 한다.

---

## 7. 오류 처리
오류는 사용자에게 이해 가능한 메시지와 개발용 로그를 분리한다.

### 사용자 메시지
- 빈 입력: `분석할 텍스트를 입력해 주세요.`
- 길이 초과: `텍스트는 2,000자 이내로 입력해 주세요.`
- AI/API 오류: `분석에 실패했습니다. 잠시 후 다시 시도해 주세요.`
- 응답 검증 오류: `분석 결과를 확인하지 못했습니다. 다시 시도해 주세요.`
- DB 저장 실패: `분석은 완료되었지만 결과 저장에 실패했습니다.`

OpenAI API 키, 전체 원문, 내부 stack trace는 사용자 메시지에 노출하지 않는다.

---

## 8. 보안 규칙
- `OPENAI_API_KEY`는 Vercel Environment Variables에 저장
- `.env`, 비밀키, Supabase service-role key를 Git에 커밋하지 않는다.
- 클라이언트에는 공개 가능한 Supabase 키만 사용하거나, MVP에서는 DB 쓰기를 서버 API에서 처리한다.
- Supabase RLS 정책을 활성화한다.
- 사용자가 입력한 텍스트를 SQL 문자열로 직접 조합하지 않는다.
- 외부 API 응답을 검증 없이 DB에 저장하지 않는다.

---

## 9. UI 구현 규칙
첨부 이미지의 다음 특성을 참고한다.
- 흰색 기반의 넓은 여백
- 짙은 네이비 텍스트
- 핑크 계열 강조색
- 둥근 pill/chip
- 큰 검색/입력 영역
- 카드 기반 결과 영역
- 상단 얇은 내비게이션
- 데스크톱에서는 넓은 2열 hero 구성
- 모바일에서는 1열로 자연스럽게 전환

단, 참고 이미지를 그대로 복제하지 않는다. 브랜드 로고, 타인의 콘텐츠 이미지, 원본 문구를 그대로 사용하지 않는다.

---

## 10. 완료 전 필수 검증
PRD의 완료기준과 `docs/06-test-and-acceptance.md`의 테스트 케이스를 모두 확인한다.

최소 확인 항목:
- positive / negative / neutral 입력
- 빈 입력
- 2,000자 초과 입력
- OpenAI 실패
- 잘못된 AI JSON
- Supabase 저장 실패
- 새로고침 후 UI 오류 없음
- 모바일 폭에서 레이아웃 깨짐 없음
- API 키가 브라우저 번들/네트워크 응답에 노출되지 않음
- Vercel 배포 환경변수로 정상 동작

---

## 11. 변경관리
기능을 추가하려면 먼저 `PRD.md` 또는 관련 `docs/*.md`를 변경한다.

코드 변경 후 다음을 기록한다.
- 무엇을 변경했는가
- 어떤 요구사항을 만족시키는가
- 어떤 테스트를 통과했는가
- 범위를 벗어난 변경은 없는가

문서와 코드가 충돌하면 코드에 맞춰 문서를 바꾸지 말고, 먼저 요구사항을 확인한다.
